// Re-export do schema tipado do banco. A fonte da verdade vive em
// `packages/shared/src/database.types.ts` (@sislog/shared/types), compartilhada
// com o portal de parceiros. Este arquivo é mantido apenas como atalho para
// os imports `@/types/database.types` já existentes no app.
export * from '@sislog/shared/types'
